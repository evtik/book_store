# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/
# $ ->
  # carouselTrunkParams = 
    # lines: 8
    # tooltip: false

  # # $('.book-description').each ->
  # # # $(document).on 'show', '.book-description', ->
    # # console.log @
    # # $(@).trunk8 carouselTrunkParams

  # # $(document).arrive '.book-description', -> $(@).trunk8 carouselTrunkParams

  # # $(document).on 'live', '.book-description', -> $(@).trunk8 carouselTrunkParams
  # $('#slider').on 'slide.bs.carousel', ->
    # console.log 'slide'
    # $('.book-description') ->
      # console.log @
      # # $(@).trunk8 carouselTrunkParams
