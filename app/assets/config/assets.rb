# %w(books cart checkout home).each do |controller|
  # Rails.application.config.assets.precompile += ["#{controller}.js.coffee"]
# end
# Rails.application.config.assets.precompile += %w(home.js.coffee)
