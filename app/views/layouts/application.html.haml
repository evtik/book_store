<!DOCTYPE html>
%html
  %head
    %meta{charset: "UTF-8"}
    %meta{name: "viewport", content: "width=device-width"}
    = csrf_meta_tags
    %title Bookstore
    %link{href: "https://fonts.googleapis.com/css?family=Open+Sans:300,400,600", rel: "stylesheet"}
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application', params[:controller]
    = yield :head
  %body
    = render 'layouts/header'
    .container{style: "visibility:#{flash[:error] ? 'visible' : 'hidden'}"}
      .alert.alert-danger.alert-dismissable{role: 'alert'}
        %button{type: 'button', class: 'close', data: {dismiss: 'alert'}, aria: {label: 'Close'}}
          %span{'aria-hidden': 'true'} &times;
        = flash[:error]
    .container{style: "visibility:#{flash[:success] ? 'visible' : 'hidden'}"}
      .alert.alert-success.alert-dismissable{role: 'alert'}
        %button{type: 'button', class: 'close', data: {dismiss: 'alert'}, aria: {label: 'Close'}}
          %span{'aria-hidden': 'true'} &times;
        = flash[:success]
    = yield
    = render 'layouts/footer'
    %script{src: "https://use.fontawesome.com/67602d7308.js"}
    -# %script{src: "../../javascripts/jquery.js"}
    -# %script{src: "../../javascripts/bootstrap.js"}
