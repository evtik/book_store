%main.container
  .text-center
    %h1.mb-30 Forgot your password?
    %p.general-password-text We'll email you instructions on how to reset it
  = form_for(resource, as: resource_name, url: password_path(resource_name),
    html: { method: :post }, html: {class: 'general-form'}) do |f|
    = devise_error_messages!
    .form-group.mb-25
      = f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'Enter your email'
    = f.submit 'Email instructions', class: 'btn btn-default center-block mb-20'
    .text-center
      = link_to 'Cancel', '/login', class: 'in-gold-500 mb-20'
